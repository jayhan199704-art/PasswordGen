# 密码生成网站 PRD（Password Forge）

## 1. 文档信息
- **产品名称**：Password Forge（密码生成器）
- **文档类型**：PRD（产品需求文档）
- **版本**：V0.1
- **状态**：草案
- **作者**：Jay / 团队
- **更新记录**：
  - 2026-02-04：创建文档

## 2. 背景与问题
用户在注册网站、创建应用账号、设置 Wi‑Fi/本地系统口令时，往往会：
- 使用弱密码（短、可预测、重复使用）
- 在不同平台复用同一密码
- 不清楚强度标准或不知道如何快速生成可用的强密码

## 3. 目标与成功指标
### 3.1 产品目标
- 提供**安全、易用、可定制**的密码生成体验
- 默认生成**高强度**、符合常见网站规则的密码
- 强化隐私：尽量在**本地生成**、不上传/不记录用户密码内容

### 3.2 成功指标（可量化）
- **生成成功率**：生成操作成功率 ≥ 99.9%
- **复制使用率**：点击“复制”占生成次数 ≥ 60%
- **可用性**：核心任务（生成并复制）平均完成时间 ≤ 10 秒
- **隐私合规**：不采集密码明文；日志/埋点不包含密码内容

## 4. 目标用户与使用场景
### 4.1 目标用户
- 普通用户：需要快速生成安全密码
- 开发/运维/安全从业者：需要批量生成、可配置策略

### 4.2 典型场景
- 注册新账号时生成强密码并复制到密码管理器
- 某些网站限制字符集/长度时按规则生成
- 批量生成一次性密码用于临时账号或测试数据

## 5. 范围定义
### 5.1 MVP 范围（优先交付）
- 单页网站生成密码（本地生成）
- 长度与字符集可配置
- 一键复制
- 强度提示（含基础校验与估算）
- “排除易混淆字符”选项
- 不保存密码到服务端

### 5.2 非范围（后续版本）
- 账号体系、云端同步、跨设备历史记录
- 密码管理器功能（存储、分类、自动填充）
- 发送密码到邮箱/短信等外部通道

## 6. 功能需求（Functional Requirements）
### 6.1 密码生成（FR-01）
- **描述**：用户可生成一条或多条随机密码。
- **默认策略**：
  - 长度：16
  - 字符集：小写 + 大写 + 数字 + 符号
  - 至少包含：每类字符至少 1 个（可开关）
  - 排除易混淆字符：开启（如 `O/0`、`l/1`、`I` 等）
- **输入项**：
  - 长度（min/max 可配置，默认 8–64；允许扩展到 128）
  - 数量（默认 1；范围 1–20）
  - 字符集开关：小写、大写、数字、符号
  - 规则开关：
    - 至少包含每类字符（on/off）
    - 排除易混淆字符（on/off）
    - 排除自定义字符（文本输入，可选）
  - 生成按钮：点击生成
- **输出项**：
  - 生成结果列表（每条可独立复制）
  - 可选：显示“显示/隐藏”切换（默认隐藏以防肩窥）

### 6.2 强度评估与提示（FR-02）
- **描述**：对当前生成密码给出强度提示，帮助用户判断是否可用。
- **展示内容（不需要绝对准确，但要一致、可解释）**：
  - 强度等级：弱/中/强/极强
  - 估算指标：字符集规模、长度、估算熵（例如 \( \log_2(|charset|^{length}) \)）
  - 规则提醒：是否缺少某类字符、是否过短
- **注意**：不进行“泄露密码检测”（如 HaveIBeenPwned）作为 MVP（避免传输/隐私问题）；后续若做需明确本地 k‑匿名或明确告知。

### 6.3 一键复制（FR-03）
- **描述**：用户可一键复制密码到剪贴板。
- **要求**：
  - 复制成功/失败提示（toast）
  - 对列表中每条密码提供“复制”按钮
  - 可选：复制后 30 秒提示用户清空剪贴板（不强制）

### 6.4 导出（FR-04，可选，V1）
- **描述**：将生成的多条密码导出为文本文件（`.txt`）。
- **要求**：
  - 文件内容包含多行密码
  - 不包含任何用户个人信息

### 6.5 历史记录（FR-05，可选，V1）
- **描述**：本地保存最近生成的 N 条记录（默认关闭）。
- **要求**：
  - 默认不保存；用户显式开启后才写入 `localStorage`
  - 提供“一键清空历史”
  - 明确提示“历史仅保存在本机浏览器”

### 6.6 多语言（FR-06，可选，V1）
- **描述**：支持中文简体/英文切换。

## 7. 交互与页面（UX）
### 7.1 页面结构（单页）
- 顶部：产品名 + 简短说明（“本地生成，不上传”）
- 左侧/上方：配置区
  - 长度滑杆 + 数值输入
  - 数量输入
  - 字符集复选框
  - 规则开关（至少包含每类、排除易混淆、排除自定义）
  - 生成按钮（主按钮）
- 右侧/下方：结果区
  - 强度条/标签 + 指标提示
  - 密码列表（每条：遮罩显示 + 显示/隐藏 + 复制）
  -（可选）导出按钮、清空按钮

### 7.2 状态与空态
- 初始空态：提示用户点击“生成”
- 错误态：
  - 未选择任何字符集：阻止生成并提示
  - 规则冲突（例如排除字符后可用字符集为空）：提示并阻止
  - 长度过小无法满足“每类至少 1 个”：提示并自动建议增大长度或关闭该规则

### 7.3 可访问性（A11y）
- 所有控件可键盘操作
- 强度提示对读屏友好（aria-label）
- 颜色对比度符合 WCAG AA

## 8. 规则与算法需求（Security/Logic）
### 8.1 随机性要求
- 必须使用浏览器安全随机源（如 Web Crypto：`crypto.getRandomValues`）
- 禁止使用 `Math.random()` 作为最终随机源

### 8.2 生成逻辑（建议）
- 先根据开关构建可用字符集（扣除“易混淆字符/自定义排除”）
- 若开启“每类至少 1 个”：
  - 先从每个被启用的类别各抽 1 个
  - 剩余长度从整体字符集中抽取
  - 最终打乱顺序（shuffle）

### 8.3 安全提示与隐私承诺
- 页面明确声明：
  - “密码在本地生成，不会上传到服务器”
  - “不要在公共场所展示密码；复制后注意剪贴板安全”

## 9. 非功能需求（NFR）
- **性能**：生成 20 条、每条 64 位密码在 100ms 内完成（常见桌面设备）
- **兼容性**：Chrome/Edge/Safari/Firefox 最近两个大版本；移动端自适应
- **可靠性**：输入校验完善；不因极端输入崩溃
- **安全**：
  - 默认启用 CSP（若有后端/静态托管配置）
  - 不输出密码到控制台日志
- **隐私**：埋点/日志不记录密码内容与用户输入的排除字符（可记录开关与长度区间等非敏感指标）

## 10. 埋点与分析（可选）
- 事件：
  - `generate_clicked`（长度、数量、字符集开关、规则开关）
  - `copy_clicked`（第几条、是否显示模式）
  - `export_clicked`
- 约束：**严禁**上报密码明文或可逆信息

## 11. 风险与对策
- **风险**：用户误以为“强度=绝对安全”
  - **对策**：文案说明强度仅为估算；建议使用密码管理器与 2FA
- **风险**：剪贴板泄露（被其他应用读取）
  - **对策**：复制提示与可选“清空剪贴板提醒”
- **风险**：规则冲突导致不可生成
  - **对策**：生成前校验并给出可操作的修复建议

## 12. 验收标准（Acceptance Criteria）
- 选择任意合法配置点击生成，必定生成符合规则的密码（长度、字符集、排除规则生效）
- 未选择字符集时不可生成并给出明确提示
- 开启“每类至少 1 个”时，生成结果满足该约束；若长度不足则提示并阻止生成
- 复制按钮可将对应密码写入剪贴板，并有成功/失败反馈
- 页面明确隐私声明：本地生成、不上传

## 13. 里程碑（建议）
- **MVP（1–3 天）**：生成 + 校验 + 强度提示 + 复制 + 基础 UI
- **V1（3–7 天）**：批量生成、导出、本地历史（可选）、多语言、完善 A11y

